Il faut installer server-json d'un facon globale : ``` npm install -g json-server ```

Apres création d'un fichier json on demarre le serveur web avec  ```json-server --watch bdd.json --port 5000 ```

